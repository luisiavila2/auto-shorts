/* ============================================================
   recipes.js — Prompts del guionista (ESQUELETO).

   Acá vive la "voz" del canal: las reglas de formato, duración,
   estructura narrativa y el esquema JSON de salida que consume
   generate-script.js.

   Se completa cuando definamos el tipo de videos del canal.
   ============================================================ */

export function buildStoryPrompt(channelContext, opts = {}) {
  // TODO: definir BASE_RULES + OUTPUT_SCHEMA según el formato del canal.
  throw new Error('recipes.js: definir buildStoryPrompt cuando se elija el tipo de contenido');
}
